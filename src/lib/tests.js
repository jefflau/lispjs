import {
  reduce,
  filter,
  map
} from './lib';
import { assertEquals } from './testFramework';

/* TESTS */

//reduce
assertEquals(reduce([1,2,3], (a, b) => a + b), 6)

console.log('hello mofossssssssssssssss')

assertEquals(filter([1, 2, 3], a => a < 2) , 6)








